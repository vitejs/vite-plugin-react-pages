{
  "name": "basic",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "vite serve src --force",
    "debug": "npx --node-arg=--inspect vite serve src --force",
    "debug:build": "npx --node-arg=--inspect vite build src --outDir dist",
    "clean": "rm -rf node_modules/.vite_opt_cache",
    "build": "vite build src --outDir dist"
  },
  "dependencies": {
    "@pika/react": "^16.13.1",
    "@pika/react-dom": "^16.13.1",
    "styled-components": "^5.2.1"
  },
  "devDependencies": {
    "vite": "npm:@csr632/vite@^1.0.0-rc.4",
    "vite-plugin-mdx": "workspace:^0.4.0",
    "vite-plugin-react": "^3.0.2"
  }
}
